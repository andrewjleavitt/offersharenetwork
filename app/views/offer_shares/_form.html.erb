<%= form_for(@offer_share) do |f| %>
  <% if @offer_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer_share.errors.count, "error") %> prohibited this offer_share from being saved:</h2>

      <ul>
      <% @offer_share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :offer_id %><br>
    <%= f.select :offer_id, options_from_collection_for_select(@offers,"id","name") %>
  </div>
  <div class="field">
    <%= f.label :advocate_id %><br>
    <%= f.select :advocate_id, options_from_collection_for_select(@advocates,"id","name") %>
  </div>
  <!-- <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
